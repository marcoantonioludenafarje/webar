{
  "name": "webar",
  "version": "0.1.0",
  "private": true,
  "description": "Technical experimentation laboratory for browser-based Augmented Reality (WebAR).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
